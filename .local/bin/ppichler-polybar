#!/usr/bin/env bash

MONITOR=`xrandr -q | grep "primary" | cut -d ' ' -f1`

if [[ -z '$MONITOR' ]]; then
  MONITOR=`xrandr -q | grep " connected" | cut -d ' ' -f1`
fi

export MONITOR=$MONITOR; polybar bottom --reload

# vi:set ft=bash
