#!/usr/bin/env sh

SELECTION="$(gopass-qt list -f | rofi -i -dmenu -matching fuzzy)"

if [ "$SELECTION" != '' ]; then
  PASS="$(gopass-qt show -o "$SELECTION")"
  echo "insert-text $PASS" >> "$QUTE_FIFO"
fi 
